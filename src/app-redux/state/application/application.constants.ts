export enum ApplicationActionType {
  UPDATE_CHAIN_ID = '[application] Update chain id',
  SET_OPEN_MODAL = '[application] Set open modal',
  ADD_POPUP = '[application] Add popup',
  REMOVE_POPUP = '[application] Remove popup',
}
