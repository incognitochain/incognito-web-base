import styled from 'styled-components/macro';

const InfoStyled = styled.div`
  margin-bottom: 24px;
`;

const ProfileInfo = () => {
  return <InfoStyled></InfoStyled>;
};

export default ProfileInfo;
