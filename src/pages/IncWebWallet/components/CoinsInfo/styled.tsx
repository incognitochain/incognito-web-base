import styled from 'styled-components/macro';

export const Container = styled.div`
  width: 100%;
  overflow-y: auto;

  .header {
    display: flex;
  }

  .body {
    display: flex;
    flex: 1;
  }
`;
